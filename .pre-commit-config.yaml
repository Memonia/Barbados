repos:
  - repo: local
    hooks:
      - id: check-sandbox
        name: check-sandbox
        entry: hooks/check-sandbox.ps1
        language: script

        # run once
        pass_filenames: false
